/// @description Insert description here
/* Runs, draws a thing using the variables of the original thing, then is destroyed.
Currently, only called by oRelationshipBar to draw units without interrupting the original unit.
*/

surfacelist = ds_list_create()