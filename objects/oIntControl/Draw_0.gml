draw_set_alpha(1)
if !instance_exists(oAttackWindow){
    if stile != 0{
        if instance_exists(stile){
            //draw_text(view_xview[0]+20,view_yview[0]+20,stile.elevation)
            }
        }
    }
//draw_text_ext(imouse_x,imouse_y,"This is a text of the tooltip system to see what it looks like when text is wrapped or line broken or whatever. It's just a line of text and there's nothing special about it.",15,150)

